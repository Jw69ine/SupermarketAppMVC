<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Supermarket App</title>
  <style>
    body { background: #f3f6fb; min-height: 100vh;}
    .center-landing {
      min-height: 88vh;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .welcome-card {
      background: #fff;
      border-radius: 18px;
      box-shadow: 0 8px 32px rgba(23,98,196,0.12);
      padding: 2.8rem 2.1rem 2.6rem 2.1rem;
      max-width: 420px;
      width: 100%;
      text-align: center;
      animation: popIn 0.37s cubic-bezier(.42,1.16,.46,.95);
    }
    @keyframes popIn {
      from {opacity: 0; transform: scale(0.97) translateY(40px);}
      to   {opacity: 1; transform: none;}
    }
    .welcome-card h1 {
      font-size: 2.2rem; font-weight: 700; letter-spacing: -0.5px; margin-bottom: 1.1rem;
    }
    .welcome-card .btn { min-width: 120px; margin: 0.25rem; border-radius: 7px; }
    .navbar { margin-bottom: 0; }
    .small-text { color: #8892b0; font-size: 1.07rem; margin-bottom: 0.9rem;}
  </style>
</head>
<body>
<%- include('partials/navbar', { user: user }) %>
  <div class="center-landing">
    <div class="welcome-card">
      <% if (user) { %>
        <h1>Welcome, <%= user.username %>!</h1>
        <div class="small-text mb-3">Role: <strong><%= user.role %></strong></div>
        <% if (user.role === "admin") { %>
          <p class="mb-3"><a href="/inventory" class="btn btn-outline-primary">Go to Inventory</a></p>
        <% } else { %>
          <p class="mb-3">Continue <a href="/shopping">Shopping</a>!</p>
          <a href="/shopping" class="btn btn-primary">Shop Now</a>
        <% } %>
      <% } else { %>
        <h1>Welcome to Supermarket!</h1>
        <div class="small-text mb-3">Best experience for online groceries.</div>
        <a href="/login" class="btn btn-primary">Login</a>
        <a href="/register" class="btn btn-outline-secondary">Register</a>
      <% } %>
    </div>
  </div>
</body>
</html>
